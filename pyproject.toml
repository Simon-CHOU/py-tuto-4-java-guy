[project]
name = "py-tuto-4-java-guy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "jupyter>=1.0",
    "nbformat>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["modules"]
